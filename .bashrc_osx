# File: ~/.bashrc_osx
# Description: Symlinked from generic ~/.bashrc_machine location
# which is sourced from ~/.bashrc

if [ -f $HOME/.bash_aliases_osx ]; then
	source $HOME/.bash_aliases_osx
fi

# Turn off warning about zsh deprecating bash
export BASH_SILENCE_DEPRECATION_WARNING=1
