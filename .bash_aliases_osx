# File: ~/.bash_aliases_osx
# Description: OSX Specific aliases
# Souced from ~/.bashrc

alias ls='/usr/local/bin/gls --color=always'
alias ll='/usr/local/bin/gls -la --color=always  --group-directories-first'
